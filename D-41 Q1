Q.Count characters in a string without using built-in length functions.

#include <stdio.h>
#include <string.h>
int main()
{
    int len=0;
    char s[100];
    printf("Enter a word: ");
    scanf("%s",s);
    for(int i=0;s[i]!='\0';i++)
    len++;
    printf("Length of string: %d",len);
    return 0;
}
